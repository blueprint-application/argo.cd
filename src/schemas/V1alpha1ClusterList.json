[
  {
    "ref": "#/components/schemas/v1alpha1ClusterList",
    "schema": {
      "description": "ClusterList is a collection of Clusters.",
      "type": "object",
      "properties": {
        "metadata": { "$ref": "#/components/schemas/v1ListMeta" },
        "items": { "type": "array", "items": { "$ref": "#/components/schemas/v1alpha1Cluster" } }
      }
    }
  }
]
