[
  {
    "ref": "#/components/schemas/v1alpha1ResourceStatus",
    "schema": {
      "title": "ResourceStatus holds the current sync and health status of a resource\nTODO: describe members of this type",
      "type": "object",
      "properties": {
        "hook": { "type": "boolean" },
        "kind": { "type": "string" },
        "name": { "type": "string" },
        "namespace": { "type": "string" },
        "health": { "$ref": "#/components/schemas/v1alpha1HealthStatus" },
        "requiresPruning": { "type": "boolean" },
        "version": { "type": "string" },
        "syncWave": { "format": "int64", "type": "string" },
        "group": { "type": "string" },
        "status": { "type": "string" }
      }
    }
  }
]
