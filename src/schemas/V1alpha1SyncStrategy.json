[
  {
    "ref": "#/components/schemas/v1alpha1SyncStrategy",
    "schema": {
      "title": "SyncStrategy controls the manner in which a sync is performed",
      "type": "object",
      "properties": {
        "hook": { "$ref": "#/components/schemas/v1alpha1SyncStrategyHook" },
        "apply": { "$ref": "#/components/schemas/v1alpha1SyncStrategyApply" }
      }
    }
  }
]
