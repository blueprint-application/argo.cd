import { SessionSessionCreateRequestModelOptions as SessionSessionCreateRequest } from '../../models';
import { BaseParams } from '../base';

export interface SessionServiceCreateParams extends BaseParams {
  body: SessionSessionCreateRequest;
}

export default {};
