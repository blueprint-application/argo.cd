import { V1alpha1ApplicationSourceModelOptions as V1alpha1ApplicationSource } from '../V1alpha1ApplicationSource';

export interface RepositoryRepoAppDetailsQueryModelOptions {
  appName?: string;
  appProject?: string;
  source?: V1alpha1ApplicationSource;
}
