import { V1alpha1ResourceDiffModelOptions as V1alpha1ResourceDiff } from '../V1alpha1ResourceDiff';

export interface ApplicationManagedResourcesResponseModelOptions {
  items?: Array<V1alpha1ResourceDiff>;
}
