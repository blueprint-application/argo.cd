import { V1alpha1AppProjectModelOptions as V1alpha1AppProject } from '../V1alpha1AppProject';

export interface ProjectProjectCreateRequestModelOptions {
  project?: V1alpha1AppProject;
  upsert?: boolean;
}
