import { V1alpha1ApplicationSetModelOptions as V1alpha1ApplicationSet } from '../V1alpha1ApplicationSet';

export interface ApplicationsetApplicationSetResponseModelOptions {
  applicationset?: V1alpha1ApplicationSet;
  project?: string;
}
